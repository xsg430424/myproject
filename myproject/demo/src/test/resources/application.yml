server:
  port: 6202
spring:
  application:
    name: goods-service    # 项目名称尽量用小写

  datasource:
    url: jdbc:mysql://localhost:3306/buz_order?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
    initialization-mode: always
    schema: classpath:sql/buz_order.sql
    data: classpath:sql/data.sql

